# Examples

| Example | Description |
|---------|-------------|
| [`standalone/`](standalone/) | A plain Go `text/template` (not Helm) converted to CUE, showing the core idea behind the tool. |
| [`simple-app/`](simple-app/) | A full Helm chart converted to a CUE module, demonstrating chart-level conversion with helpers, values, and multiple templates. |
