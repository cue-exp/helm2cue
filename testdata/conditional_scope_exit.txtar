Conditional block that exits parent list scope.

-- values.yaml --
tls: true
-- input.yaml --
apiVersion: v1
kind: Pod
metadata:
  name: test
spec:
  containers:
  - name: test
    image: busybox
    args:
    {{- if .Values.tls }}
      - "--enable-tls=true"
      - "--port=443"
    ports:
      - containerPort: 443
    {{- else }}
      - "--enable-tls=false"
    ports:
      - containerPort: 8080
    {{- end }}
-- helm_output.yaml --
apiVersion: v1
kind: Pod
metadata:
  name: test
spec:
  containers:
  - name: test
    image: busybox
    args:
      - "--enable-tls=true"
      - "--port=443"
    ports:
      - containerPort: 443
-- output.cue --
