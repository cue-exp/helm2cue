Template directive ({{ template }}) with a field expression argument.
No round-trip validation: Go's {{ template }} writes directly to output,
so it cannot be used inline in "key: {{ template ... }}" for execution.

-- _helpers.tpl --
{{- define "id" -}}
{{ . }}
{{- end -}}
-- input.yaml --
name: {{ template "id" .input.name }}
-- output.cue --
#input: {
	name!: bool | number | string | null
	...
}
_id: {
	#arg: _
	#arg
}

output: [
	{
		name: _id & {#arg: #input.name, _}
	},
]
